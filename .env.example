DOMAIN=onebonus.am
ACME_EMAIL=vectoritllc@gmail.com
BACKEND_UPSTREAM=app:8080

# Published Spline scene URL for the hero card-merge animation (public, build-time).
# When set, the landing uses the Spline 3D scene; otherwise it falls back to the CSS scene.
# VITE_SPLINE_MERGE_URL=https://prod.spline.design/XXXXXXXX/scene.splinecode
